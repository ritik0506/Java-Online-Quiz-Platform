# Database Configuration
# Copy this file to .env and update with your database credentials

DB_URL=jdbc:mysql://localhost:3306/quiz_platform?useSSL=false&allowPublicKeyRetrieval=true
DB_USER=root
DB_PASS=your_password_here

# Application Settings
# Context path defaults to /quiz-platform-full based on pom.xml finalName
